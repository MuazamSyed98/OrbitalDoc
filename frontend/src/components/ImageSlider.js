import React from "react";

const ImageSlider = () => {
  return (
    <div style={{ padding: "1rem", backgroundColor: "#f0f0f0", borderRadius: "8px" }}>
      <h3>🖼️ Before/After Image Comparison</h3>
      <div
        style={{
          width: "100%",
          height: "300px",
          display: "flex",
          alignItems: "center",
          justifyContent: "center",
          border: "2px dashed #ccc",
        }}
      >
        <p style={{ color: "#999" }}>Image slider placeholder</p>
      </div>
    </div>
  );
};

export default ImageSlider;
